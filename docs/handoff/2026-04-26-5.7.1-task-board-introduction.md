权威设计：docs/V5_ROADMAP.md v5.7.1 段「TASK_BOARD 引入 + 文档治理重构」

# Handoff: 5.7.1 — TASK_BOARD 引入 + 文档治理重构

## 1. 目标（功能语言 — Jack 会看到什么）

- 项目根多一份 `docs/TASK_BOARD.md`，回到项目打开即看到当前所有进行中 / 待规划 / 待拍板任务的横切总览
- 完成事件不再分散在 ROADMAP `[x]` + handoff/README 已归档表 + docs/README.md Development Log 三处，统一进 `docs/history/INDEX.md` 永久全量倒序索引
- `docs/handoff/README.md` + `docs/history/TECH_DEBT_BACKLOG.md` 两份低维护价值文档消失
- 项目 CLAUDE.md + 全局 workflow.md 配套规则切换，新出 handoff / 接手会话 / 归档动作按新落点 + 状态机执行

## 2. 文件改动清单

### 2.1 新建（2 份）

- `docs/TASK_BOARD.md`
- `docs/history/INDEX.md`

### 2.2 删除（2 份）

- `docs/handoff/README.md`（整文件，含原 5 类内容拆解去向见 §3）
- `docs/history/TECH_DEBT_BACKLOG.md`（现存 1 条迁入 TASK_BOARD「待拍板」段后删）

### 2.3 修改（项目仓库 2 份）

- `docs/README.md`（落点表 / Root-level 表 / history 表 / Development Log 段 共 4 段）
- `CLAUDE.md`（Handoff Pointer Registry / Session Startup tmp / Session Closeout / Roadmap Reference 共 4 处）

### 2.4 修改（dotfiles 仓库 1 份）

- `~/.claude/docs/workflow.md`（§9.a 指针登记 + §9.a 末追加分工表样板 + §9.c 第 2 步 + §9.d 段尾 14 天提示 + 新增 §9.f 共 5 处）

### 2.5 不可动

- `docs/handoff/` 文件夹本身（保留，handoff 待消费裸落点）
- `docs/handoff/archive/` 文件夹本身（保留，已归档 handoff 全文存档）
- 任何 `docs/history/v*_*.md`（不动，本任务不重排既有 history 文件）
- 任何 `docs/handoff/archive/*.md` 内既有 handoff 文件（不动，仅做索引迁移）
- `docs/V5_ROADMAP.md`（5.7.1 条目已在本会话落盘 `[~]`，本任务不再动 ROADMAP）
- 全局 `~/.claude/CLAUDE.md` + `~/.claude/docs/dispatch.md`（Codex 终审已确认无引用旧落点，仅需 grep 确认无残留）
- 任何 `Sources/` / `Tests/` / `specs/` 代码与规范（本任务纯文档治理）

## 3. 执行步骤

### Step 1 — 准备 TASK_BOARD 内容（已按真实状态填好，接手会话直接套用）

「进行中」段共 3 行：

- **5.1.5**：ROADMAP s1-s7 标「✅ 已归档」，s8（设计稿+handoff）+ s9（按 s8 handoff 执行）剩余。s8 当前无 handoff 文件 → 状态：`需求提出 (s8)`，指针 → ROADMAP
- **5.1.6**：ROADMAP `[~]` 已规划设计（杠杆 A/B/C/重构），具体执行 handoff 待 5.1.5 收尾后启动 → 状态：`需求提出`，指针 → ROADMAP
- **5.7.1**：本任务。接手会话开工后状态翻为「方案执行中 (5.7.1)」 → 起手值：`方案完成 (5.7.1)`，指针 → 本 handoff

其它版本段（5.0.x / 5.2.x / 5.3.x / 5.4.x / 5.5.x）现状：所有 active 条目均已 `[x]` 完成，无新 `[~]` / `[ ]` 待办 → 不进 TASK_BOARD。

「待拍板」段共 1 行：从 `docs/history/TECH_DEBT_BACKLOG.md` 迁 LibraryStore try? audit 剩余项。

### Step 1.5 — 跳过（无遗留 mv 需要补）

s5/s6/s7 的 handoff 文件已在 `docs/handoff/archive/` 中（由 5.1.5 分支历史 commit 完成 git mv）。本任务无需补做任何历史归档动作，直接进 Step 2。

### Step 2 — Commit 1（项目原子，吸收 Codex 终审 F1）

执行顺序（关键：所有改动在同一 commit 内，消除"规则指向不存在文件"中间态）：

1. **建 `docs/TASK_BOARD.md`** — 按 §4 schema 模板写骨架，填 Step 1 决定的内容
2. **建 `docs/history/INDEX.md`** — 按 §5 schema 模板写骨架，迁入：
   - `docs/README.md` 当前 Development Log 表 6 条（v5.0.0 / v5.3.1 / v5.3.2 / v5.3.3 / v5.3.4 / v5.5.0）
   - `docs/handoff/README.md` 当前「已归档」表全部条目（按完成日倒序）
3. **改 `docs/README.md`** —
   - 「项目侧落点」表：➕TASK_BOARD 行 ➕history/INDEX 行 ➖TECH_DEBT 行 ➖handoff/README 指针描述
   - 「Root-level」表：➕TASK_BOARD 行
   - 「history」表：➕INDEX 行 ➖TECH_DEBT 行
   - 删「Development Log」整段（已迁入 INDEX）
4. **改项目 `CLAUDE.md` 4 处** — 按 §6 改文清单逐字替换
5. **`git rm docs/handoff/README.md`**
6. **`git rm docs/history/TECH_DEBT_BACKLOG.md`**
7. **`git status --short` 自检** — 确认 staged 文件仅含上述 6 项预期改动
8. **Commit 1**: `docs(task-board): introduce TASK_BOARD + history/INDEX, retire TECH_DEBT_BACKLOG + handoff/README, wire CLAUDE.md`

### Step 3 — Commit 2（dotfiles 仓库，独立 repo）

9. 改 `~/.claude/docs/workflow.md` 5 处，按 §6 改文清单
10. grep 全局 `~/.claude/CLAUDE.md` 与 `~/.claude/docs/dispatch.md` 是否有 `TECH_DEBT_BACKLOG` / `handoff/README` 引用（预期无）
11. `cd ~/dotfiles && git add -A && git status` 自检 — 确认仅 workflow.md 一处 staged
12. **Commit 2** in dotfiles: `docs(workflow): generalize handoff index pointer + add §9.f task-board triggers + tmp 14d prompt + 执行分工 sample`
13. `git push`（dotfiles，CLAUDE.md [HARD]）

### Step 4 — 验收（见 §7 checklist）

## 4. TASK_BOARD.md 完整文件（按真实状态，接手会话直接落盘）

```markdown
# Task Board — 全局任务总览

> **职责**：跨版本展示当前所有进行中 / 待规划任务的流水线状态。一句话 + 状态 + 指针，仅此而已。
>
> **不装**：
> - 设计思路 → `V5_ROADMAP.md`（进行中条目）/ `history/v*_*.md`（已归档）
> - 实施方案 → `handoff/<file>.md`（待消费）/ `handoff/archive/<file>.md`（已执行）
> - 验收摘要 + commit 哈希 → `history/v*_*.md` 实施摘要段 + `history/INDEX.md` 索引
>
> **维护规则**：见 `~/.claude/docs/workflow.md §9.f` 触发表。

## 进行中

| 版本 | 一句话 | 状态 | 指针 |
|---|---|---|---|
| 5.1.5 | 规则管理统一 + 自动同步基础设施 (s1-s7 已完成) | 需求提出 (s8) | [V5_ROADMAP §5.1.5](V5_ROADMAP.md) |
| 5.1.6 | Codex 派发提速基建（杠杆 A/B/C + 重构方案设计） | 需求提出 | [V5_ROADMAP §5.1.6](V5_ROADMAP.md) |
| 5.7.1 | TASK_BOARD 引入 + 文档治理重构 | 方案完成 (5.7.1) | [handoff/2026-04-26-5.7.1-task-board-introduction.md](handoff/2026-04-26-5.7.1-task-board-introduction.md) |

## 待拍板

| 来源 | 描述 | code pointer |
|---|---|---|
| Round E | LibraryStore try? audit 剩余项（createDirectory + read paths 仍 try? 静默吞错；目标态：stderr 落日志或 throw；effort: low）| `Sources/SpinLabApp/Library/LibraryStore.swift` |
```

**接手会话开工后**：5.7.1 状态从「方案完成 (5.7.1)」翻为「方案执行中 (5.7.1)」（首次 commit 落地时）。

## 5. history/INDEX.md 完整文件（按真实数据，接手会话直接落盘）

数据来源已核对：`ls docs/history/` + `ls docs/handoff/archive/` + ROADMAP「Development Log」表。

```markdown
# History Index

> 永久全量。按完成时间倒序。每条事件归档时同步追加（§9.c 第 3 步内子动作）。
>
> 替代原 `docs/README.md`「Development Log」表 + 原 `docs/handoff/README.md`「已归档」表两份冗余索引。

| 完成日 | 版本 | 一句话 | history 文件 | handoff archive | commits |
|---|---|---|---|---|---|
| 2026-04-26 | 5.1.5-s7 | rules tail cleanup：WorkflowDefinitionStore + WorkflowRegistryRetirementService + parentID 兼容删 + RulesBootstrapper 替换 RulesMigration + WorkflowRegistryView 只读重写；11 tests（79/79 V515 green）| — | [archive/2026-04-26-5.1.5-s7-rules-tail-cleanup.md](handoff/archive/2026-04-26-5.1.5-s7-rules-tail-cleanup.md) | 5 commit (08c7f8a–f7939c4) |
| 2026-04-26 | 5.1.5-s6 | 自动同步引擎：RepositoryPointer + RulesSyncEngine + degraded UI + 32 tests；68/68 V515 green | [v515_s6_auto_sync_engine.md](history/v515_s6_auto_sync_engine.md) | [archive/2026-04-26-5.1.5-s6-auto-sync-engine.md](handoff/archive/2026-04-26-5.1.5-s6-auto-sync-engine.md) | 99514e2 + 4a586fe |
| 2026-04-26 | 5.1.5-s5 | 规则面板重写：RulesManagementStore + 5 新 section + WorkflowMatchRuleEditor + 36 tests；R1 硬门禁满足；旧 5 section 文件全删；swift build clean | [v515_s5_rules_panel_rewrite.md](history/v515_s5_rules_panel_rewrite.md) | [archive/2026-04-26-5.1.5-s5-rules-panel-rewrite.md](handoff/archive/2026-04-26-5.1.5-s5-rules-panel-rewrite.md) | ea09161 |
| 2026-04-26 | 5.1.5-s4 | 5 本子 schema 落地 + RuleLoader/RulesMigration 重写 + WorkflowIDAllocator/parentID/rotationHintRules 退役 + 旧 bundle 文件删除 + V210 fixture 更新；swift build clean + 测试全绿 | [v515_s4_schema_migration.md](history/v515_s4_schema_migration.md) | — (handoff 文件位置不可考) | — |
| 2026-04-26 | 5.1.5-s3 | s3 双盲对抗 Round 2：盘点 + 5 本子分类设计稿 + 退役调用点全清单 + 新 schema 草案；C1-C6 commit 链 | — | [archive/2026-04-26-5.1.5-s3-rules-redesign.md](handoff/archive/2026-04-26-5.1.5-s3-rules-redesign.md) | 8df3ee7 + a9ebf19 + f527d17 + 362025b + 1a62ed8 + 747b7e4 |
| 2026-04-26 | 5.1.5-s2 | 规则面板搭骨架：6 分区 NavigationSplitView + 4 编辑分区 + Inbox 入口 + MatchRuleEditor/RegexField；swift build clean | — | [archive/2026-04-26-5.1.5-s2-rules-panel.md](handoff/archive/2026-04-26-5.1.5-s2-rules-panel.md) | 494ab17 + 3fb92ac + a8cf29a + e583534 |
| 2026-04-26 | s1-fixture-fallout | 测试 fixture 改名收尾：路径改名 + V515 隔离 + V210/V223/V214/V224/V225/V240 完整修复；swift test 全绿 | — | [archive/2026-04-26-s1-fixture-fallout-test-fixture-rename.md](handoff/archive/2026-04-26-s1-fixture-fallout-test-fixture-rename.md) | 80959a1 + a6edeca + 2ee4d6c + 10c7d47 |
| 2026-04-25 | 5.1.5-s1 | 规则统一：旧规则 UI 三处删除 + 7 文件 schema 落地 + 一次性迁移器 + sync 脚本删除；swift build clean | — | [archive/2026-04-25-5.1.5-s1-rules-unification.md](handoff/archive/2026-04-25-5.1.5-s1-rules-unification.md) | 63c891a→5b10d0f |
| — | 5.5.0 | Cross-Area UI Unification（字体可读性 / 折叠区块 / 字体梯度 / 间距常量 / 按钮风格 6 项）| [v550_ui_unification.md](history/v550_ui_unification.md) | — | — |
| — | 5.3.4 | Legend Dimension Auto-Inference + Visual Consistency（resolver + payload + 16 tests）| [v534_legend_dimension_resolver.md](history/v534_legend_dimension_resolver.md) | — | — |
| — | 5.3.3 | Multi-Tab Render State Manager（TabRenderState + TabRenderOutput + 3ω/XY/AHE 三 store 状态重组）| [v5.3.3_multi_tab_render_manager.md](history/v5.3.3_multi_tab_render_manager.md) | — | — |
| — | 5.3.2 | Plot Render Pipeline unification（WorkbenchRenderPipeline 三 workflow 共用）| [v5.3.2_render_pipeline.md](history/v5.3.2_render_pipeline.md) | — | — |
| — | 5.3.1 | Plot Shell capability expansion（绘图模式 / 字号 / tick 密度 / 辅助线 / Copy PNG）| [v5.3.1_plot_shell.md](history/v5.3.1_plot_shell.md) | — | — |
| — | 5.0.0 | Knowledge accumulation system setup（philosophy / features / known_issues / devlog 系统化）| [v5.0.0_doc_system.md](history/v5.0.0_doc_system.md) | — | — |
```

**说明**：
- 「完成日」按 handoff archive 文件名日期 / commit 日；纯 history 文件无明确归档日的填 `—` 占位
- 「history 文件」/「handoff archive」缺一是因为：(a) 早期 v5.0/v5.3.x 完成时未走 handoff 流程，无 archive；(b) 5.1.5-s3/s2/s1/s1-fixture 当时未生成独立 history 文件，设计思路集中在 ROADMAP 5.1.5 段；(c) s4 handoff 文件位置不可考

## 6. 规则文档逐条改文清单

### 6.a — 项目 `CLAUDE.md`「Handoff Pointer Registry (overrides global workflow.md §9.a)」段

**当前**：

```
- [HARD][must] SpinLab 的 handoff 指针登记落点是 **`docs/handoff/README.md`** 的「待消费」表，不是全局规则里的 `docs/ledger/l1_优化待办.md`（本项目无 ledger 体系，用 V5_ROADMAP + history 替代）。
- 产出 handoff 时：(1) `mv` 草稿到 `docs/handoff/<YYYY-MM-DD-topic>.md`，(2) 在 README 索引「待消费」段加一行。归档时反向迁移到「已归档」段。
```

**改后**：

```
- [HARD][must] SpinLab 的 handoff 指针登记落点是 **`docs/TASK_BOARD.md`** 的「进行中」表（不是全局规则里的 `docs/ledger/l1_优化待办.md`，本项目无 ledger 体系）。
- 产出 handoff 时：(1) `mv` 草稿到 `docs/handoff/<YYYY-MM-DD-topic>.md`，(2) 在 TASK_BOARD「进行中」表对应行翻状态为「方案完成 (s<n>)」+ 指针列改指 handoff 文件。归档时（§9.c）整行删 + history/INDEX 加一行。
```

### 6.b — 项目 `CLAUDE.md`「Knowledge Accumulation > Session Startup」段第 2 项

**当前**：

```
- `[HARD][must]` Run `ls tmp/` and triage residue per `docs/handoff/README.md`（留 / 升级到 docs/handoff / 删）.
```

**改后**：

```
- `[HARD][must]` Run `ls tmp/` and triage residue per `~/.claude/docs/workflow.md §9.d` tmp lifecycle rules（留 / 升级到 docs/handoff / 删；超 14 天默认提示 Jack）.
```

### 6.c — 项目 `CLAUDE.md`「Knowledge Accumulation > Session Closeout (event-driven)」段

在当前 5 项列表后追加第 6 项：

```
  6. 是否动了流水线状态（出 handoff / 第一次 commit / 归档完成）？→ 同步翻 `docs/TASK_BOARD.md` 状态或删行；归档时同步在 `docs/history/INDEX.md` 加一行。详见 `~/.claude/docs/workflow.md §9.f` 触发表。
```

### 6.d — 项目 `CLAUDE.md`「Roadmap Reference」段第 2 项

**当前**：

```
- Discovered bugs/debt: 先进 `docs/history/TECH_DEBT_BACKLOG.md` Inbox；Jack 拍板归入版本段时再剪到 ROADMAP（避免双账本）。
```

**改后**：

```
- Discovered bugs/debt: 先进 `docs/TASK_BOARD.md`「待拍板」段；Jack 拍板归入版本段时迁入「进行中」段（状态 =「需求提出」），同步从「待拍板」删（避免双账本）。
```

### 6.e — 全局 `~/.claude/docs/workflow.md §9.a`「指针登记」段

**当前**：

```
**指针登记**：handoff 写完后在项目侧约定的 handoff 索引（如 `docs/handoff/README.md`「待消费」表，或项目 CLAUDE.md 指定的其它落点）登记一行：`[ ]` + handoff 文件相对路径 + 破折号 + 一句话说明。
```

**改后**：

```
**指针登记**：handoff 写完后在项目侧约定的 handoff 索引登记（落点由项目 CLAUDE.md 指定，可能是 task board / handoff README 待消费表 / 其它项目 ledger）。登记动作至少：handoff 文件相对路径 + 一句话说明 + 状态/checkbox 标记。
```

### 6.f — 全局 `~/.claude/docs/workflow.md §9.a` 末尾追加「执行分工表样板」子段

在 §9.a 最后一句「handoff 写完，当前会话告诉 Jack：handoff 位置 + 复制哪段启动指令。」之后插入：

```markdown

#### 执行分工表样板（必含字段示例）

\`\`\`markdown
## 执行分工
| 任务 | 负责 AI | 依赖 | 可否并行 | 备注 |
|---|---|---|---|---|
| Commit 1: <动作> | Claude | — | 否（先做） | <层/原因> |
| Commit 2: <动作> | Codex | C1 完成 | 是（与 C3a 并行） | <层> |
| Commit 3a: <动作> | Claude | C1 完成 | 是 | <层> |
| Commit 3b: <动作> | Codex | C2 完成 | 否 | <层> |
\`\`\`
```

（注：实际写入时 `\`\`\`` 替换为三个反引号。）

### 6.g — 全局 `~/.claude/docs/workflow.md §9.c` 第 2 步

**当前**：

```
2. **handoff 索引更新**：从「待消费」段剪切对应行 → 粘贴到「已归档」段（或项目约定的归档索引位置），补上 commit 哈希 / 执行会话标识 / 一句话验收摘要
```

**改后**：

```
2. **handoff 索引更新**：从项目侧 handoff 索引按项目约定迁移（删行 / 翻状态，落点由项目 CLAUDE.md 指定）。归档详情（commit 哈希 / 执行会话标识 / 一句话验收摘要）同步追加到项目约定的归档索引（如 history/INDEX / handoff/README「已归档」表）。
```

### 6.h — 全局 `~/.claude/docs/workflow.md §9.d` 段尾

**当前最后一句**：

```
下次会话开工时若仍有 tmp/ 残留 → 按项目级 `docs/handoff/README.md` 三选一规则处置（留 / 升级到 docs/handoff/ / 删）。
```

**改后**：

```
下次会话开工时若仍有 tmp/ 残留 → 按以下三选一处置：

- 还在用（当前对抗期内）→ 留着，告诉 Jack 是什么、为什么还要
- 对抗已收敛但 handoff 未落盘 → `mv` 到 `docs/handoff/` 并按 §9.a 登记
- 已废弃 / handoff 已落盘 → `rm`（草稿无保留价值）

超过 14 天没动的 tmp 文件，**默认提示 Jack 是否删除**。
```

### 6.i — 全局 `~/.claude/docs/workflow.md` 新增 §9.f

在 §9.d 之后、§10（如有 §9.e 的话则在 §9.e 之后）之前插入：

```markdown
### §9.f TASK_BOARD-style 任务总览维护触发表（项目侧可选）

如项目 CLAUDE.md 引入跨版本任务总览文件（如 SpinLab 的 `docs/TASK_BOARD.md`），按以下触发矩阵维护，与 §9.a / §9.b / §9.c 联动：

| 触发动作 | 总览文件动作 |
|---|---|
| 新债 / raw 捕获 | 「待拍板」段加行 |
| Jack 拍板归入 ROADMAP 版本段 | 「待拍板」段删行 + 「进行中」段加行（状态 =「需求提出」）|
| handoff 落盘（§9.a） | 状态翻「方案完成 (s<n>)」+ 指针列改指 handoff 文件 |
| 接手会话第一次 commit 落地（§9.b） | 状态翻「方案执行中 (s<n>)」 |
| §9.c 归档（5 步原步骤后） | 整行删 + 永久归档索引加一行（如项目侧 history/INDEX）|

**重做规则**：执行中冒出 handoff 未覆盖的设计问题 → 按 §9.b 停下报 Jack → Jack 显式指挥触发重做（作废旧 handoff / 出新 handoff）。AI 不自行回退状态。

具体落点（哪个文件、哪一段）由项目 CLAUDE.md 指定。本规则给出维护时机与状态机，不绑定具体落点。

**Sanity check（[HARD]，§9.c 收尾后执行）**：归档动作 5 步完成后，必须运行 `ls docs/handoff/` 自检 —— 现役区应只剩刚出未消费的 handoff 文件；如发现 ROADMAP 已 `[x]` 的条目对应 handoff 仍在现役区，说明 §9.c 第 1 步（git mv 到 archive/）遗漏，立即补做。
```

## 7. 验收 checklist

- [ ] `docs/TASK_BOARD.md` 存在；含「进行中」「待拍板」两段；schema 与 §4 一致
- [ ] `docs/history/INDEX.md` 存在；含原 docs/README.md Development Log 6 条 + 原 handoff/README「已归档」表全部条目；按完成日倒序
- [ ] `docs/handoff/README.md` 已删除（`ls docs/handoff/` 不再含 README.md）
- [ ] `docs/history/TECH_DEBT_BACKLOG.md` 已删除；其内容已迁入 TASK_BOARD「待拍板」段
- [ ] `docs/README.md`：「项目侧落点」表 + 「Root-level」表 + 「history」表均已更新；「Development Log」段已删
- [ ] 项目 `CLAUDE.md` 按 §6.a / 6.b / 6.c / 6.d 改完
- [ ] Commit 1 单一 commit 同时包含上述 6 项（项目侧原子）
- [ ] `~/.claude/docs/workflow.md` 按 §6.e / 6.f / 6.g / 6.h / 6.i 改完
- [ ] dotfiles `git push` 成功（CLAUDE.md [HARD]）
- [ ] grep 全仓 `TECH_DEBT_BACKLOG\|handoff/README\.md` 仅在 `docs/handoff/archive/` 内的旧 handoff 历史里出现，docs/ 现役 + 项目 CLAUDE.md + 全局 workflow.md 均无残留
- [ ] §9.c 归档动作 5 步完成（含 tmp/ 清理本任务对抗草稿）
- [ ] TASK_BOARD「进行中」段 5.7.1 行整行删 + history/INDEX 加 5.7.1 行

## 8. 执行分工

| 任务 | 负责 AI | 依赖 | 可否并行 | 备注 |
|---|---|---|---|---|
| Commit 1 全部步骤 | 单方 | — | 否（原子 commit） | 文档治理纯文本任务，无需双 AI 分工，单方完成全部 §2.2 + §2.3 改动 |
| Commit 2 全部步骤（dotfiles） | 单方 | C1 完成 | 否 | 同上，dotfiles 改动单方完成 |
| 代码评审（§4.c） | 另一方 | C1 + C2 完成 | — | 评审拟改条款是否与 ROADMAP 5.7.1 设计思路一致、grep 验证无残留 |

注：本任务无代码改动，纯文档治理。§4.c 评审形态为「另一方读完最终 diff + 验证 §7 checklist 全勾」。

## 9. 启动指令

复制以下 prompt 到新 terminal 启动接手会话：

```
按 docs/handoff/2026-04-26-5.7.1-task-board-introduction.md 执行。
权威设计在 docs/V5_ROADMAP.md v5.7.1 段。

关键约束：
1. Commit 1 必须原子 — 项目侧建文件 / 删文件 / 改 docs/README.md / 改项目 CLAUDE.md 4 处全部在同一 commit，不允许中间态出现"规则指向不存在文件"
2. Commit 2（dotfiles 仓库）独立操作，与 Commit 1 时序解耦但必须当天完成 + push（CLAUDE.md [HARD] dotfiles 同步）
3. TASK_BOARD「进行中」段当前已知至少含 5.1.5（按实际状态填）+ 5.7.1（接手会话开工后翻为「方案执行中」）。其它版本段按 V5_ROADMAP.md 实际有 [~]/[ ] active 条目则填，无则不填
4. 任何执行中的疑问 → 停下报 Jack。设计已收敛，不重做方案
5. 完成后按 §9.c 5 步归档：handoff git mv → TASK_BOARD 5.7.1 行整行删 + history/INDEX 加一行 + 设计思路从 ROADMAP 5.7.1 剪到 history/v571_task_board.md + ROADMAP 5.7.1 改 [x] + 一句话 + 链接 + tmp/ 清理本任务全部对抗草稿（claude-plan / codex-plan / claude-review-codex / codex-review-claude / converged-plan / codex-review-brief-* / codex-review-converged 全删）
```
