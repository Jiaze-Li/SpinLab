# Handoff: 5.1.16 A 包 — Test Sync Debt（G1-G4 测试跟进源码）

**版本**：5.1.16 | **范围**：A 包（4 项测试同步债，与 B 包独立 handoff/独立 PR）
**主笔**：Claude 方 | **Acceptance review**：Codex
**前置文档**：`docs/V5_ROADMAP.md` 5.1.16 段；`tmp/2026-05-05-5116-baseline.md`（事实层证据）；`tmp/2026-05-05-5116-rhythm-codex-review.md`（节奏对抗结论）
**产品方向（Jack 拍板 2026-05-05）**：4 项全选 X — 测试跟着源码改，源码方向不动

---

## §1 范围与禁止

### Scope Allowed（仅这些可改）
- `Tests/SpinLabAppTests/V413AHEMultiSeriesExtractionTests.swift`
- `Tests/SpinLabAppTests/V5111ExtractAHEMetricsUseCaseTests.swift`
- `Tests/SpinLabAppTests/V515RulesEngineRegressionTests.swift`
- `Tests/SpinLabAppTests/V513LibraryFeatureStoreFacadeTests.swift`
- `Tests/SpinLabAppTests/V221DrawerMatchEngineTests.swift`

### Read-Only References（G3 实施时定位现行 API 用，禁止修改）
- `Sources/SpinLabApp/App/State/LibraryFeatureStore.swift`
- `Sources/SpinLabApp/App/State/LibraryFeatureStore+DrawerSelection.swift`

仅供 G3 查证现行公开 selection action 名称；任何 Sources 修改属越界。

### Scope Denied
- 禁止修改 `Sources/**` 任何 swift 文件（产品方向 X = 测试跟源码、源码不动；Sources 仅 Read-Only References 列出的两文件可读）
- 禁止扩大到其他测试文件
- 禁止变动 ROADMAP / TASK_BOARD 之外的文档（除本 handoff 进 archive 时由归档者搬迁）
- 禁止把 4 项 squash 成单 commit；每个 G 单独 commit 便于事后 git blame
- 每个 G 实施开始前 + commit 前必跑 `git status --short`；如出现非 Scope Allowed 文件 halt 报告

---

## §2 子任务（4 项 + 4 个独立验收 filter）

### G1 — V413/V5111 AHE metric（4 issues）

**根因**：源码 `ExtractAHEMetricsUseCase.extractAHEMetricsPerSeries` 已改为读 `WorkbenchPlotSeries.sampleID`（typed sampleID 字段，5.3.6 引入），不再从 label 解析。测试 helper 仍用 `WorkbenchPlotSeries(label: "\(sampleKey) | ch1", ...)` 把 sampleKey 拼进 label，没有传 `sampleID:`，所以 `s.sampleID = nil` → 整 label 进 failedLabels。

**修法**：测试 helper 在构造 `WorkbenchPlotSeries` 时显式传入 `sampleID: sampleKey` 参数；label 仍可保留 `"sampleKey | channel"` 形态（label 仅用于显示，不影响 metric 提取）。

**修改点**：

`Tests/SpinLabAppTests/V413AHEMultiSeriesExtractionTests.swift` `makeAHESeries` helper（line 20-47）：

```swift
return WorkbenchPlotSeries(
    label: label,
    x: xs,
    y: ys,
    sampleID: sampleKey   // 新增此行
)
```

unparseable case（line 127-139 `unparseableLabelReturnsFailure`）：`bad` 不传 sampleID（保持 nil），label 置空 → 仍进 failedLabels。已成立的 expectation `["<empty>"]` 不变。

`multipleUnparseableLabels`（line 141+）同处理：bad1/bad2 不传 sampleID。

`Tests/SpinLabAppTests/V5111ExtractAHEMetricsUseCaseTests.swift` `makeAHESeries` helper（line 13-17）：同 V413，加 `sampleID: sampleKey`。

**保留**：`parseSampleKey` 函数源码侧仍在，V413 line 49-83 + V5111 line 30-34 的 `parseSampleKey` 单测继续通过（与 metric 提取路径解耦），不动。

**验收 filter**：

```bash
swift test --filter V413AHEMultiSeriesExtractionTests
swift test --filter V5111ExtractAHEMetricsUseCaseTests
```

两个 suite 全 pass。

---

### G2 — V515 RulesEngine（3 issues）

**根因**：源码 5.1.5 规则统一后，filename rule parser 对温度/磁场做大写化 + 单位换算 + 半步四舍五入。测试 expectation 仍按"保留原始字面量"写。

**修法**：测试 expectation 改成大写化+换算后的结果。

**修改点**（`Tests/SpinLabAppTests/V515RulesEngineRegressionTests.swift`）：

| 行 | 当前 expectation | 改为 |
|---|---|---|
| 133 | `"300k"` | `"300K"` |
| 157 | `"500mT"` | `"0.5T"` |
| 183 | `"4.5K"` | `"5K"` |

验收口径：expectation 字面值匹配，不要求测试解释归一化算法。

**Test name 同步**（行 128 + 152 + 177 的 `@Test("...")` 描述应反映新 expectation；line 177 测试名 `"half-step normalization rounds 4.5K to 4.5K"` 改为 `"half-step normalization rounds 4.5K to 5K"`）。

**验收 filter**：

```bash
swift test --filter V515RulesEngineRegressionTests
```

整个 suite 28 tests 全 pass（其中 25 已通过、3 修后 pass）。

---

### G3 — V513 LibraryFeatureStoreFacade（1 case × 4 issues）

**根因**：源码 5.1.13b audit C1-C8 把 LibraryFeatureStore 的 `librarySelectedPrefix` / `librarySelectedBatchId` / `librarySelectedSampleId` / `libraryActiveSelectionSource` 四个属性的 didSet observer 删除，改为显式动作（如 `selectExistingDrawer` / `cancelPendingSelectionChange` 等）内手动调 `onPersistInteractionSnapshot` callback。测试还按"赋值即触发"写。

**修法**：

实施前先读 Read-Only References 中的两个 Sources 文件，定位**现行公开**会触发 `onPersistInteractionSnapshot` callback 的 selection action（5.1.13b 把 trigger 集中到 1-2 个显式动作；5.1.15 god-file 拆分可能改了文件位置，未必改了 API 名）。

**修改点**（`Tests/SpinLabAppTests/V513LibraryFeatureStoreFacadeTests.swift`，Suite "LibraryFeatureStore facade — callback wiring"，line 347-376）：

替换 `selectionDidSet_firesCallback` test case：用现行公开 selection action（如 `selectExistingDrawer(...)` / `cancelPendingSelectionChange()` 等，**实施时按 Read-Only References 查证结果填**）driving，断言 callback 被调。原"赋值递增 callCount"的形态从测试里去除。

**伪代码**（实施时按现行 API 替换 `<currentSelectionAction>`）：

```swift
@Test("explicit selection actions fire onPersistInteractionSnapshot")
func explicitSelectionActions_fireCallback() {
    let store = TestFixtures.makeStore()
    var callCount = 0
    store.configureFacade(
        // ... 同原 configureFacade，persistInteractionSnapshot: { callCount += 1 }
    )

    store.<currentSelectionAction>(...)
    #expect(callCount >= 1)
}
```

**[HARD] G3 不允许降级为"断言 callCount==0"**：这种降级只证明 didSet 被删，不验证替代持久化路径成立，验收价值低于最低线。如 Read-Only References 查证后找不到合适的现行公开 action 来 driving callback —— **halt，报告查证到的所有 public API 与阻塞点，不交付**。

**验收 filter**：

```bash
swift test --filter V513LibraryFeatureStoreFacadeTests
```

全部 34 tests pass（33 已通过 + 1 改后 pass）。

---

### G4 — V221 DrawerMatchEngine（1 issue）

**根因**：源码 v4 substrate schema 改造把 `BAKED` 的 canonical token 从全词 `baked` 改成短码 `b`。测试 expectation 仍写全词。

**修法**：expectation 改为短码 + 注释更新。

**修改点**（`Tests/SpinLabAppTests/V221DrawerMatchEngineTests.swift` line 8-18）：

```swift
@Test("shared semantic descriptor canonicalizes fields consistently")
func sharedSemanticDescriptorCanonicalizesConsistently() {
    let descriptor = SampleSemanticDescriptor(
        batch: "pn32",
        processingTokens: ["BAKED", "o", "HF"],
        material: "sto",
        orientation: "111"
    )
    // v4 substrate schema: "BAKED" canonicalizes to short code "b"
    #expect(descriptor.canonicalKey == "PN32|HF+b+o|STO|111")
}
```

注释更新仅限本测试 case 内文字；不要扩展到 schema 文档、源码常量、其他测试。

**验收 filter**：

```bash
swift test --filter V221DrawerMatchEngineTests
```

全部 9 tests pass（其中 line 21+ 的 exact-match-uniquely / fallback-subset-match 已用 `b`，符合新 schema，不动）。

---

## §3 执行节奏

1. **每个 G 单独 commit**：commit message 规范 `fix(5.1.16-G<n>): <一句>`，例 `fix(5.1.16-G1): test fixtures pass typed sampleID instead of label-encoded sampleKey`
2. **commit 前必跑该 G 的 filter**，pass 后才 commit
3. **commit 前 `git status --short`** 全列入；如 status 出现非 §1 Scope Allowed 的文件，halt 并报告
4. **4 个 G 全完后** 再跑一次 4 个 filter 串行确认互不污染

---

## §4 Acceptance Review（Codex 责任）

Codex 在 Claude 完成 4 个 commit 后，独立做：

1. **Diff 评审**：每个 commit 是否仅碰 §1 Scope Allowed 文件 / 是否 squash 合规 / commit message 是否反映 G 编号
2. **filter 验证**：独立跑 4 个 filter，确认全 pass + 无新失败
3. **节奏完整性**：4 个 G 是否都覆盖 baseline.md 中列的所有 issues（G1=4 / G2=3 / G3=4 / G4=1，共 12 issues）
4. **独立判断**：是否有 Claude 漏掉的边缘 case 或将"测试跟源码"做成"测试只是糊"（例如 G3 改成单纯断言 callCount==0 而非补充显式动作触发）

输出 `tmp/2026-05-05-5116-A-acceptance.md`：adopt / adopt-with-fixes / reject + must-fix（如有）。

---

## §5 失败 fallback

- 任一 filter 实施后仍有红 → halt，报 Jack，不强行交付
- G3 找不到现行公开 selection action 来替换占位 → halt，报告已查证的 public API 列表与阻塞点；**不允许降级为仅断言 callCount==0**（无验收价值）

---

## §6 完成定义

- [ ] 4 个 commit 落地（git log 可见 G1-G4 编号）
- [ ] 4 个 filter 独立跑均 pass（output 落 tmp/）
- [ ] Codex acceptance review 报告产出（tmp/）
- [ ] Codex 裁决 adopt 或 adopt-with-fixes（fix 全并入）
- [ ] TASK_BOARD A 包行状态翻「方案执行中」（s2 落盘后由 Claude 翻）→ A PR 合后翻「验收中」→ 5.1.16 closeout 时整体删
- [ ] PR 提交（A 包独立 PR，B 包另开）

**TASK_BOARD 同步状态（s1/s2 落盘时点）**：
- s1 落盘（2026-05-05）：A 包行已翻「方案完成 (s1)」+ 指针 → 本 handoff
- s2 落盘（adopt-with-fixes 修订并入）：状态升级为「方案完成 (s2)」（仍未启动 Executing）
- 第一次 commit 落地：状态翻「方案执行中 (s2)」
